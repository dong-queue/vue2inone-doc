export default {
  items: [
    { component: 'swc', model: 'switch1', label: { value: 'switch1', func: (e) => `Switch 1: ${e.toString()}`}},
    { component: 'swc', model: 'switch2', label: { value: 'switch2', func: (e) => `Switch 2: ${e.toString()}` } }
  ]
}